# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# API Configuration
API_HOST=0.0.0.0
API_PORT=5000

# CORS Configuration (for Render deployment)
ALLOWED_ORIGINS=https://*.render.com,http://localhost:3000,http://127.0.0.1:5000

# Claims Processing Configuration
DAMAGE_THRESHOLD=25000
FRAUD_KEYWORDS=fraud,inconsistent,staged,suspicious,falsified
